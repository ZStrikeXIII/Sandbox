public class CellPhone implements Item { //Implements means we need to use Item's method(s).

  // Okay only if abstract. We extend an abstract class.
  public void printItemInfo() {
    //implement is now satisfied.
  }

}
